import React from 'react';
import { StyledButton, StyledDeleteIcon } from './Button.styles';

const Button = () => (
  <StyledButton>
    <StyledDeleteIcon />
  </StyledButton>
);

export default Button;
